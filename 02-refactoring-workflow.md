# 重构实施流程与回退方案

状态：用户于 2026-09-20 审核通过并授权启动 P0；合仓/裁剪尚未启动。当前模型收敛为 GLM-5.2-w4a8c8，2P2D、TP8/DP2，共 32 张 910B3。执行证据见 [P0](p0/README.md)，总体设计见 [01](01-refactoring-design.md)，验收用例见 [03](03-baseline-and-validation.md)。

## 1. 执行前提

审核与 P0 启动授权已经获得。当前四仓 HEAD 是输入边界；开工时重新检查分支和工作区，若有新增修改，将其作为用户资产保存并更新基线，不覆盖、不切回旧提交。P0 出口仍须取得实际证据，启动授权不等于阶段通过。

当前模型边界为 GLM-5.2-w4a8c8 原生文本生成，保留已有 MTP、DSA，GLM-5.3 后续扩展；其他完整模型、多模态及 Qwen/Llama 蒸馏不在保留范围。GLM-5.2 所需 DeepSeek/MTP/Eagle 组件按依赖保留，不开放额外模型入口。当前分支目标模型相关的 CPU KV 卸载、跨实例缓存、P/D 分离及恢复路径继续保留并回归验收。

首批环境为 Ascend 910B3、CANN 8.5.1、torch 2.9.0、torch_npu 2.9.0；4 节点每节点 8 卡，2P2D、TP8/DP2，按 P、D 各两路 TP8 副本解释。GLM-5.2-w4a8c8 的实际 revision/config/hash、量化提供方、C8 布局、EP/PP/CP 和通信配置仍需内网补齐；缺少这些细节不将必保能力改为可选项。

构建与验证在内部局域网完成，可通过 proxy 访问互联网，但禁止直接接入外部大模型/AI 助手服务；不再按完全断网环境设计。内网 torch 已为 2.9.0，直接使用现有环境，不安排重装或升级。本文所有扩展编译、sdist/wheel 构建、安装和 NPU 验收步骤均在内网执行；本机只做源码工作、静态检查，以及现有工具条件下不需要构建的 host 逻辑测试。

实施输入包括：批准的功能范围、实际模型/权重清单、硬件和依赖 profile、可运行的离线/在线/缓存启动配置、验收负载以及接受的性能波动门槛。没有 NPU 和模型时可以推进代码整理及静态检查，但不能宣告运行等价或完成生产交付。

建议在两个主体仓的重构分支/隔离 worktree 上实施，保留四个原始快照作为对照。最终只交付两个活动仓库；阶段中的原始 checkout 和离线备份不构成新增产品仓。

### 1.1 本机与内网协作

| 位置 | 工作 | 交接结果 |
| --- | --- | --- |
| 本机工作区 | 源码重构、依赖/注册/构建脚本静态检查、迁移清单、构建和验收脚本编写；代理配置仅提供占位说明 | 配对源码版本、完整源码材料、依赖差异清单和校验值 |
| 内网构建环境 | 人员或内网 CI 核对实际环境，按策略经代理/内网源补齐依赖，编译扩展、生成 sdist/wheel、安装和检查 ABI | 内网保留完整环境/构建记录，交接经审核脱敏的报告、产物校验值和失败定位信息 |
| 内网 NPU 验收环境 | 人员或内网 CI 执行目标模型、离线/在线服务、跨实例缓存、P/D、恢复和性能回归 | 原始日志、trace 和性能数据留存内网，交接与配对版本对应的审核后验收结果 |

每阶段分别记录“源码就绪”和“内网验证通过”。未获得内网结果时可继续不依赖该结果的源码整理，但不能把阶段验证标为通过、删除恢复基线或宣告整体完成。当前会话不直接连接或远程控制内网验证环境，也不要求在该环境部署大模型客户端或 Agent；代理可联网不改变这一协作边界。内网目标模型推理与服务测试不属于被禁止的外部大模型接入。

交接闭环为：本机提交配对源码/脚本及说明 → 内网人员审核并执行或提交内网 CI → 完整证据内网留存 → 人工审核、脱敏并提供允许外发的最小结果 → 本机分析并生成下一批修改。自动向外部上传日志、云端 AI 诊断或大模型驱动的远程终端均不纳入方案；内网 CI 制品收集、日志汇聚和监控不受此限制。无法外发的材料由内网人员分析后提供允许披露的结论，不以绕过限制换取排障能力。

## 2. 阶段总表

| 阶段 | 主要工作 | 产物 | 通过条件 |
| --- | --- | --- | --- |
| P0 | 冻结范围、提交、环境与行为基线 | 来源/环境清单、模型支持表、基准报告、依赖/补丁清单 | 能重现批准的当前功能；失败项有明确处置 |
| P1 | 合仓、统一构建、保留临时命名空间 | 两个可安装分发包，Ascend 当前快照已导入 | 四包安装变为两包安装，批准的基线场景等价 |
| P2 | vLLM 原生 Ascend | 原生平台、Runner、算子、图、通信及模型补丁 | 基础推理不依赖外部 Ascend 插件；该阶段相关功能对照通过 |
| P3 | LMCache 原生 Ascend | 原生 CacheEngine、NPU Connector、配置、扩展和传输 | 导入/安装补丁消失，缓存和恢复链路等价 |
| P4 | 依赖闭包驱动的裁剪 | 模型白名单、非目标代码和依赖已移除 | 所有批准场景可达；未批准模型/设备/任务不能加载 |
| P5 | 两仓联合资格验证 | 可安装 wheel、镜像、兼容清单、功能/性能报告 | [03](03-baseline-and-validation.md) 中所有交付门槛通过 |
| P6 | 切换与归档 | 双仓工作区、操作手册、可恢复备份 | 从干净环境只用两个包复现，旧仓归档可验证恢复 |

每阶段有稳定的配对版本；阶段失败则停在该阶段修复或回退，不带着未知回归继续大规模删代码。

## 3. P0：建立可对照的基线

### 3.1 来源与恢复准备

记录每仓 HEAD、分支、tree、工作区状态和本地需要保护的未跟踪文件。对当前分支可达历史创建可恢复的离线 Git bundle 或等价备份，并验证恢复到临时目录后 tree 一致；不额外吸收其他分支内容。

主体仓保留自身历史。Ascend 插件以当前树快照导入，记录原仓、commit、许可证和新旧路径映射；不复制插件的 `.git`。默认不做四个仓库的整体历史重写。若用户需要完整作者历史，再调整导入策略。

### 3.2 环境与模型

以已确认的 910B3 / CANN 8.5.1 / torch 2.9.0 / torch_npu 2.9.0 建立首批 profile，在内网读取实际机器信息，经审核脱敏后提供报告；torch 2.9.0 已安装，无需重新安装。补齐 Python、操作系统、CPU 架构、卡数、内存、驱动/固件、triton-ascend、编译器、C++ ABI、Mooncake/通信库版本以及构建镜像摘要。依赖材料依据内网信息选择，不能使用本机环境代替；同时确认代理生效工具、允许访问的来源、CA 和内网直连范围，报告不包含认证秘密。

每个验收模型记录权重位置/版本、config hash、`architectures`、dtype、量化、tokenizer/chat template、TP/EP/PP/DP 配置。将“源码存在”“基线可运行”“已通过本次验收”分列，避免把 registry 当支持承诺。

将当前源码中的 torch 2.10.0 构建声明改为匹配现有内网 2.9.0 环境，默认关闭自动构建隔离，核对 API 和扩展 ABI；若四仓当前配置无法共同运行，先形成面向该环境的最小源码/构建修复及原因记录。这些修复独立提交，与结构搬迁分离；不把修复解释为升级、降级或重装内网 torch。已有缺陷不能静默当作期望行为；批准范围中的关键缺陷未解决时不得通过发布门槛。

核对 CANN 8.5.1 下现有构建选择的 HIXL/hcomm one-sided 通道与所安装 SDK、910B3 的实际能力，分别验证推理 HCCL 集合通信和缓存传输。显式冻结通道配置与依赖，不以版本号推定某个传输组合已通过。

### 3.3 依赖和补丁清单

实施时生成完整清单，至少包含：

- Python AST 导入及继承、`super()` 和未覆盖方法、动态 import 字符串、entry points、注册表。
- 所有当前启用及目标配置可触发的 patch：目标符号、加载条件、作用顺序、实际新行为、目标归属、验证用例。
- CMake/setup 扩展、pybind 模块名、共享库和包资源、测试 conftest 的设备假设。
- 目标模型的 tokenizer/config/renderer/reasoning/tool parser、量化、spec decode 和权重映射闭包。
- DSA / MTP / RemoteFill / checkpoint 的跨仓调用与数据字段。

输出 `source-manifest.json`、`migration-map.json`、`patch-inventory.json`、`support-matrix.json` 等评审/执行产物。P0 首批材料已写入 [p0/](p0/README.md)，支持矩阵统一使用标准库可读取的 JSON；静态候选、人工语义核对和内网实测分别标记，文件清单不直接构成删除许可。

### 3.4 固定对照负载

至少建立：无缓存离线/在线、CPU KV 卸载与缓存冷/热、目标 MLA/DSA、GLM 结构化共享 indexer、不等 KV 分组、MTP、图执行，以及已确认保留的跨实例缓存、P/D 分离、RemoteFill/checkpoint 和恢复路径。

固定输入 token、采样配置、权重、并行配置、并发度、输出长度和 seed，采集输出、TTFT/TPOT/ITL、吞吐、峰值 HBM/RSS/共享内存、加载字节、cache hit、异常和重启情况。基线标记来自实测或旧报告，旧报告不能替代本次重现。

P0 出口：确定可复现的支持矩阵；不能运行的组合明确归类为需要修复、范围外或待硬件验证，禁止伪装成通过。已确认的必保场景不能因未提供机器、拓扑或测不到而被改列范围外；缺少对应证据时验收保持未完成。

### 3.5 代理联网环境的构建交付与反馈

以下为执行流程；P0 材料的实际完成状态以 [工作记录](p0/README.md) 为准：

1. 内网生成环境和工具链报告、已安装依赖清单，人工审核脱敏后交接必要内容；本机据此生成完整依赖约束和缺失项清单。torch 2.9.0 等符合要求的已安装组件不列为重装项。
2. P0 交付四仓原始 Git bundle、独立修复差异及来源/校验清单；缺少的子模块源码按固定 gitlink commit 补齐，bundle 不冒充子模块载荷。P1 合仓后改为两个固定版本的完整源码包。普通源码打包可在本机完成；调用 build backend 的 sdist/wheel 生成均在内网完成。
3. 对缺失项生成匹配内网 Python ABI、CPU 架构、操作系统、CANN/torch 的依赖获取清单，内网人员或 CI 优先从内网制品库获取，也可经 proxy 从允许的源下载 wheel、第三方源码和必要工具。固定版本/commit/digest、校验文件并缓存；离线 wheelhouse 可选，不要求本机下载或编译全部依赖。
4. 内网先校验源码材料并运行只读预检：解释器与依赖路径、torch 版本/构建信息、torch_npu、CANN、编译器和必要通信库；核对各工具代理、CA、NO_PROXY 及本地模型客户端目标地址。缺失项在单独的依赖准备步骤补齐，不由构建时的隐式升级解决；代理失败且缓存不齐时停止，不回退公网直连。大模型接入限制通过配置和出站策略审查确认，不为测试限制而实际调用外部大模型服务。
5. 在已激活且依赖齐全的内网环境中，使用预装构建工具生成产物。默认非隔离构建且关闭 pip 自动依赖解析；构建工具确需获取的固定第三方材料可按设计经代理下载并缓存。sdist 重新展开后的构建也要验证，不依赖开发目录的 `.git`、未声明缓存或未打包资源。
6. 将两个新 wheel 安装到内网测试环境。这里的“干净环境”指没有旧四包、旧 editable 路径和工作区导入污染，并复用相同的既有基础环境或其副本，不要求重装 torch。两仓产物安装关闭自动依赖安装，之后检查依赖、扩展和 torch 身份不变。
7. 内网保留完整命令、环境报告、日志、trace 和测试结果；按交付批次整理源码/产物校验值、用例编号、退出码、结果摘要及必要错误片段。脱敏并人工审核后，仅提供内部策略允许外发的部分；去除代理凭据、访问令牌、敏感地址和业务样本，不自动回传给大模型服务。失败时按配对版本修正源码，生成新交付批次；区分“依赖/代理未就绪”“编译失败”“运行失败”“证据待审核”与“验收通过”。

构建命令示意（仅在内网、迁移后的各仓根目录分别执行；已通过内网源或代理准备好前置依赖，本轮未执行）：

```bash
python -m pip --disable-pip-version-check wheel --no-index --no-deps --no-build-isolation --wheel-dir ./dist .
```

这里的 `--no-index` 用于已备齐 Python 依赖后的构建步骤，不代表整个内网必须断网；它也不能阻止显式 URL 或 setup/CMake 自身发起网络请求。交付前检查全部下载路径：删除非目标依赖下载，保留资源经内网源或明确配置的代理获取并锁定。使用全新构建目录及已声明的缓存/依赖来源复现，验证代理异常不会触发公网直连。完整材料的断网重建可作为补充检查，不再是强制验收门槛。保留内网与回环通信，避免将 P/D、缓存及分布式测试流量送往公网代理。

## 4. P1：先合仓和构建

1. 以当前 `vllm`、`LMCache` 各自分支建重构分支。
2. 将两个 Ascend 当前源码树导入相应主体仓，保留版权来源；临时允许一个 wheel 中包含主体和 `_ascend` 两个命名空间。
3. 每仓合并为唯一 packaging/build 入口，统一版本、requirements、CMake 目标和包资源。迁入自定义 op 的配置、头文件、动态库和安装路径。
4. 建立复用内网 torch 2.9.0 的 NPU 构建 profile，停止默认安装 GPU 专用依赖；允许目标依赖按锁定清单从内网源或经代理获取，不能依靠下载 CUDA wheel 解决缺失符号。
5. 按 3.5 节将配对源码交付内网，构建并安装两个新 wheel，确认没有残留的旧插件 distribution、editable 路径和工作区 `PYTHONPATH` 帮助导入。
6. 在内网执行 P0 的代表场景，保留入口、参数、输出和扩展加载结果，经审核脱敏后交接必要证据。

本阶段的“双命名空间”是迁移手段，不是最终交付。不要把目录搬迁、Runner 重写和全部模型删除放在一个提交里。

## 5. P2：vLLM 原生化

按以下顺序推进，每个步骤都带源符号到新符号的映射和对应测试：

1. 原生 `NPUPlatform`、统一配置和量化注册、明确的设备初始化；去除 Ascend 平台发现前提。
2. 先融合平台层 patch：KV cache spec、worker metadata 聚合、通信、GLM tool parser、调度相关有效行为。无关模型 patch 先移出加载清单，待 P4 删除。
3. 盘点 GPU Runner 的继承契约，提取公共状态/输出/input batch；形成不继承 GPU Runner 的 NPU 主路径。
4. 合入 NPU Attention/MLA/SFA、算子、采样、量化、图管理、HCCL/并行；逐项去除 OOT 替换。
5. 合入 DeepSeek MTP、weight loader 和 rejection sampler 相关 patch，保留既有 DSA/缓存 callbacks 及恢复行为。
6. 消除 `torch.cuda` wrapper 和 GPU 模块函数替换；核对 eager、capture、replay、混合 prefill/decode、MTP 的实际路径。
7. 将通信、sparse offload、配置和相关工具迁入主体命名空间，清除 `vllm_ascend` 导入和插件 entry points。

P2 出口：无 LMCache 时离线和在线目标模型可运行；启用 LMCache 的联合基线仍通过。不能以“无缓存正常”为由通过 DSA/MTP 相关搬迁。GPU 文件可以等 P4 统一删除，但生产路径此时必须不再依赖它们的设备实现。

当前 AGENTS.md 中的插件开发约定需同步更新为新的原生开发约定；用户批准的合仓目标优先于继续通过 patch 适配上游的旧工作方式。保留签名、来源、测试及许可证规范。

## 6. P3：LMCache 原生化

1. 合并配置定义和验证，清除运行时重新生成配置类及旧引用修补。
2. 明确 NPU/CPU 内存、KV layout 和 group metadata；将 Ascend C++ 扩展正式构建为目标模块。
3. 将 `GPUConnectorInterface` 中仍有用的接口抽出并原生实现 NPU Connector；保留 host registration、异步事件和 slot mapping。
4. 按方法逐步合并 CacheEngine 与 Ascend 子类，保留当前主体分支的缓存 key、异步存储、checkpoint、RemoteFill 和不等分组逻辑。
5. 合并 vLLM adapter 与 Ascend adapter；把能力声明和生命周期方法接入正式 Connector，不再修改 vLLM 已定义类。
6. 合入 storage factory、P2P/PD、IPC、传输通道、hash、lookup、NUMA 和 RPC 路径修正；保留已有配置禁配规则。
7. 独立验证 RemoteFill 的 discovery、destination sealing、首次 prepared load、并发请求、超时和成对重启要求。
8. 删除 import 时 patch、`sys.modules` 替换、全局 `transfer_to_npu` 和安装后 patch，清除 `lmcache_ascend` 模块依赖。

P3 出口：两包导入顺序不再影响行为，关闭 LMCache 不要求它安装；CPU KV 卸载、跨实例缓存、既有 P/D 和恢复链路按确定的部署配置全部通过。不能保留一个空 `lmcache_ascend` 壳来掩盖遗漏导入并称为完成。

## 7. P4：范围裁剪

### 7.1 先锁入口，再删文件

安装模型架构、任务、设备、量化和 Connector 的批准白名单；移除可绕过白名单的自动 fallback。对不支持项建立明确的负向验证。

负向验证明确覆盖 DeepSeek/GLM 多模态模型、DeepSeek-R1 的 Qwen/Llama 蒸馏模型，以及图像/音频/视频输入；离线、在线及 draft 加载入口都不能绕过限制。

按 `GlmMoeDsa -> deepseek_v2`、`glm_moe_dsa -> deepseek_mtp`、`mtp -> eagle_proposer -> llama_eagle3` 等当前目标闭包先提取共用实现，保持权重命名和计算语义；不再为范围外 Dense GLM/ChatGLM 保留完整模型。随后删除非目标模型文件、注册项、配置、parser/template、测试、示例和文档。

### 7.2 分批删减

| 批次 | 内容 | 删除前的依赖条件 |
| --- | --- | --- |
| A | 其他设备平台、Worker、通信、Attention、量化 kernel、CUDA/HIP 源码 | P2/P3 原生化完成；NPU import/build 闭包不引用这些实现 |
| B | 多模态、基于 Qwen/Llama 的蒸馏模型及其他非目标模型和专用组件 | 模型范围已确认；目标文本架构、MTP、parser 与权重加载共享依赖已提取 |
| C | RLHF/训练协作 API、weight update/transfer | 与普通权重加载、跨机模型分片加载和推理控制面区分完成 |
| D | 经另行范围审核决定删除的 pooling、LoRA、SGLang/MindSpore、CacheBlend 等 | 对应决策已确认；公共类型/入口已解耦 |
| E | 不使用的存储后端、旧 API/历史适配器、重复 Runner | 当前 profile 与新接口闭包没有调用；历史版代码中的共用工具已迁移 |
| F | 不再匹配的依赖、Dockerfile、CI、测试 fixtures、示例和文档 | 对应功能实现已删除，删除清单可追溯 |

每批次后检查静态引用、延迟导入、package data、wheel 内容和对应 smoke。对保留文件中的 AMD/NVIDIA 分支也要裁剪，不能只删除 `platforms/cuda.py` 就宣称去除了其他设备支持。

不直接按 `gpu`、`cpu`、`triton`、`llama` 关键词批量删除。允许短期旧字段兼容，但发布前所有残留都需解释归属；版权、迁移说明及负向测试中的关键词不算设备实现残留。

## 8. P5：联合验收和发布候选

1. 在内网全新构建目录中按固定清单生成两个 sdist/wheel；依赖与固定 digest 的基础镜像可从内网仓库或经代理获取。使用无旧四包污染、基础 torch 环境保持不变的内网环境验证安装，不以完全断网为前提。
2. 检查依赖闭包、来源/校验值、`pip check`、动态库、ABI、唯一模块来源、两仓版本配对及 torch 未被替换；核对代理路由、大模型接入限制和证据审核流程符合 [01 第 8 节](01-refactoring-design.md)。
3. 跑 [03](03-baseline-and-validation.md) 的分层验证：静态/host、NPU kernel、模型、服务、分布式、KV/恢复、性能和长稳。
4. 对 GLM-5.2-w4a8c8 的固定 checkpoint、逐层量化、C8/MTP 及所有必保场景分别给出结果；一次正常输出不能代替整个支持矩阵，GLM-5.3 不据此获得认证。
5. 生成 `compatibility.json`（两仓版本、环境、API/cache schema）、支持矩阵、配置迁移表、删除清单、许可证来源和操作手册。
6. 在内网保留基线和候选的测试原始记录、benchmark 参数、统计方式、日志及图执行证据；仅按 3.5 节提供审核后的必要结果。未覆盖的硬件/组合标为未认证，不默认为支持。

性能门槛未通过时先定位差异，回到相关提交修复；本轮目标是保行为的结构精简，不能用待完成的后续优化抵消当前回归。

## 9. P6：最终切换和回退

切换前完成一次恢复演练：从保存的当前四仓快照和旧环境描述恢复基线，并验证模型/配置/权重仍可访问。模型权重不是 Git 备份的一部分，需要单独记录位置与校验信息。

新版本部署采用停止接入、等待/终止在途请求、确认异步 store 和通信完成、成对切换两仓构建的顺序。RemoteFill 存在已武装传输或要求 paired restart 时，遵循其既有故障协议，不能只重启一个组件。

涉及 cache schema/布局变化时使用新缓存 namespace，保留旧 namespace 供旧版本回退；不要让新旧版本共享无法互读的持久化对象。只有经验证兼容的场景才复用旧缓存。

完成干净安装和验收后，将 `vllm-ascend/`、`LMCache-Ascend/` 两个原始 checkout 做可恢复归档并退出活动工作区，保留两个主体仓。不要在 P1 就永久删除原始源码和 Git 数据。归档不打包进产品 wheel/镜像。

### 9.1 阶段回退粒度

| 问题 | 回退方式 |
| --- | --- |
| 路径搬迁/注册丢失 | 回退对应独立提交到上一配对里程碑，保留已验证的其他变更 |
| NPU 算子/ABI 不匹配 | 使用上一完整构建 profile 的两个 wheel 和镜像，不混装扩展 |
| Connector/缓存布局不匹配 | 成对回退两仓和配置，切回兼容 namespace |
| 性能回归 | 用固定负载定位到迁移批次；不靠关闭原有必保功能通过 |
| 原始分支就有失败 | 记录为基线问题；在批准范围内独立修复并重建对照 |

## 10. 提交和验收记录

建议按 `build: 双仓构建`、`refactor: 平台原生化`、`refactor: Runner 解耦`、`refactor: CacheEngine 合并`、`refactor: 目标模型共享组件`、`chore: 非目标代码裁剪`、`test/docs: 联合交付` 分组。每组再按可独立验证的模块拆分。

每个提交记录：源 commit/符号、目标路径、行为是否改变、受影响的场景、测试与未测项。跨仓变更使用同一迁移编号和配对 commit 清单。保留原许可证、版权和原仓要求的提交签名，不伪造测试结果。

完成判定：两个仓库、两个可安装包、原生 NPU 执行、批准模型白名单、其他设备/训练协作功能实际移除、离线在线及必保缓存功能通过、性能达标、旧仓可恢复归档，全部成立后才交付“重构完成”。
